get create project# 订阅设计

解压缩算法

```js

const key = "flutter_movie"

function createRandomString(len) {
  // 返回随机字符串
}

function encode(raw) {
  const _ =  base64.encode(raw)
  let hash = md5.encode(_, key)
  const len = hash.len

  // 取余 9
  const offsetSize = len % 9

  // 映射表 => 1 = 'a'
  [1,2,3,4,5,6,7,8,9,0, 'a','b','c','d','e','f', 'g', 'h', 'i', 'j']

  return offsetSize + hash  
}

function decode() {

}



```