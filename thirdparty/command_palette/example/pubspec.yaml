name: command_palette_example
description: Command Palette Example

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.12.0 <3.0.0"
dependencies:
    flutter:
        sdk: flutter

    command_palette:
        path: ../
    flutter_lorem: ^2.0.0

dev_dependencies:
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
